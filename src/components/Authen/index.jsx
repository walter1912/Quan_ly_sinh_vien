import React from 'react';
import PropTypes from 'prop-types';

const Authen = props => {
    return (
        <div>
            check Authen
        </div>
    );
};

Authen.propTypes = {
    
};

export default Authen;