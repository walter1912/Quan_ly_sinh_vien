import React from 'react';
import PropTypes from 'prop-types';

const Loading = props => {
    return (
        <div>
            page are loadding...
        </div>
    );
};

Loading.propTypes = {
    
};

export default Loading;